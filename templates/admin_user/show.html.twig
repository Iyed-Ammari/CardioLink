{% extends 'dashboard/base_dashboard.html.twig' %}

{% block body %}

<h2>👤 Profil de {{ user.nom }} {{ user.prenom }}</h2>

<a href="{{ path('admin_user_index') }}">← Retour liste</a>

<div style="margin:20px 0;">
    {% if user.imageUrl %}
        <img src="{{ user.imageUrl }}" alt="Photo" style="width:100px; height:100px; border-radius:50%; object-fit:cover;">
    {% else %}
        <div style="width:100px; height:100px; border-radius:50%; background:linear-gradient(135deg,#F82239,#2F60F5); display:flex; align-items:center; justify-content:center; color:#fff; font-size:36px; font-weight:800;">
            {{ user.nom|first }}
        </div>
    {% endif %}
</div>

<table border="1" style="margin-top:20px;">
    <tr><th>Email</th><td>{{ user.email }}</td></tr>
    <tr><th>Téléphone</th><td>{{ user.tel }}</td></tr>
    <tr><th>Adresse</th><td>{{ user.adresse }}</td></tr>
    <tr><th>Rôle</th><td>{{ user.roles|join(', ') }}</td></tr>
    <tr><th>Inscription</th><td>{{ user.createdAt ? user.createdAt|date('d/m/Y H:i') : '-' }}</td></tr>
    <tr><th>Statut</th><td>{{ user.isActive ? '✅ Actif' : '🔒 Bloqué' }}</td></tr>
</table>

<br>
<a href="{{ path('admin_user_edit', {id: user.id}) }}">✏️ Modifier</a>

{% endblock %}