{% extends 'base.html.twig' %}

{% block title %}Modifier le Rendez-vous{% endblock %}

{% block body %}
<div class="container mt-5">
<<<<<<< HEAD
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-header bg-warning text-dark">
                    <h3 class="mb-0">✏️ Modifier le Rendez-vous</h3>
                </div>
                <div class="card-body">
                    {# Affiche les erreurs globales #}
                    {% for message in app.flashes('danger') %}
                        <div class="alert alert-danger">
                            {{ message }}
                        </div>
                    {% endfor %}

                    {{ form_start(form) }}
                       
                        <div class="mb-3">
                            <label class="form-label">{{ form.medecin.vars.label }}</label>
                            {{ form_widget(form.medecin, {'attr': {'class': 'form-select'}}) }}
                            {% if form.medecin.vars.errors %}
                                <div class="invalid-feedback d-block">
                                    {% for error in form.medecin.vars.errors %}
                                        {{ error.message }}
                                    {% endfor %}
                                </div>
                            {% endif %}
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label class="form-label">{{ form.dateHeure.vars.label }}</label>
                                {{ form_widget(form.dateHeure, {'attr': {'class': 'form-control', 'type': 'datetime-local'}}) }}
                                {% if form.dateHeure.vars.errors %}
                                    <div class="invalid-feedback d-block">
                                        {% for error in form.dateHeure.vars.errors %}
                                            {{ error.message }}
                                        {% endfor %}
                                    </div>
                                {% endif %}
                            </div>
                           
                            <div class="col-md-6 mb-3">
                                <label class="form-label">{{ form.type.vars.label }}</label>
                                {{ form_widget(form.type, {'attr': {'class': 'form-select'}}) }}
                                {% if form.type.vars.errors %}
                                    <div class="invalid-feedback d-block">
                                        {% for error in form.type.vars.errors %}
                                            {{ error.message }}
                                        {% endfor %}
                                    </div>
                                {% endif %}
                            </div>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">{{ form.remarques.vars.label }}</label>
                            {{ form_widget(form.remarques, {'attr': {'class': 'form-control', 'rows': '3', 'placeholder': 'Ex: Douleurs thoraciques depuis 2 jours...'}}) }}
                        </div>

                        <div class="mt-3 d-flex justify-content-between">
                            <a href="{{ path('app_rdv_index') }}" class="btn btn-secondary">Retour</a>
                            <button class="btn btn-warning">💾 Enregistrer les modifications</button>
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
=======
    <h1>✏️ Modifier le Rendez-vous</h1>
   
    <div class="card shadow-sm mt-4">
        <div class="card-body">
            {{ form_start(form) }}
                {# On réutilise les widgets, ou on laisse Symfony faire #}
                {{ form_widget(form) }}
               
                <div class="mt-3 d-flex justify-content-between">
                    <a href="{{ path('app_rdv_index') }}" class="btn btn-secondary">Retour</a>
                    <button class="btn btn-warning">💾 Enregistrer les modifications</button>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>

{# LE SCRIPT EST NÉCESSAIRE ICI AUSSI #}
<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Attention : Vérifie bien l'ID de ton champ Type (souvent rendez_vous_type)
        const selectType = document.getElementById('rendez_vous_type');
        // L'ID du champ Lieu généré par Symfony (souvent div > label > select)
        // Astuce : Cherche le div parent du champ Lieu.
        // Si tu as utilisé form_widget(form), c'est automatique.
        // Si tu veux le cibler précisément, il vaut mieux personnaliser l'affichage comme dans new.html.twig
       
        // Pour faire simple ici, on suppose que tu utilises le même template que New
        // Si tu veux faire pro, copie-colle tout le contenu de new.html.twig ici
        // et change juste le titre et le texte du bouton.
    });
</script>
>>>>>>> b6f51606fe623e32b207fad7a4244e37179d11cb
{% endblock %}
